import * as React from "react"

// Placeholder — Task 28 replaces this with router-based wiring.
export default function App(): React.JSX.Element {
  return <div className="p-8 text-slate-600">AgentLabX test shell — router pending (Task 28)</div>
}
